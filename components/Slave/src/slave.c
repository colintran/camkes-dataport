#include <camkes.h>
#include <mytype.h>
#include <stdio.h>
#include <string.h>

#define TRUE 1
#define M_TICK 1000500

/*int run(void) {
  //while (!*(volatile char*)s1);
  //printf("Pong: received %s\n", (volatile char*)s1);

  //printf("Pong: sending %s...\n", world);
  //strcpy((void*)s2->data, world);
  double time=0; 
  int turn=0;
  while(TRUE){
    ++time;
    if (time == M_TICK){
      time %= M_TICK;
      ++turn;
      //Display
      printf("Buffer:\nname:\t%s\ngender:\t%d\nturn:\t%d\n",s1->name,\
                                                  s1->gender,s1->turn);
    }
    if(turn == 10) break;
  }

  printf("Exit...\n");
  return 0;
}*/
